/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: { unoptimized: true },
  // output: 'export'  ← descomenta para desplegar como sitio estático (GitHub Pages)
};
export default nextConfig;
